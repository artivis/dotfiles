export PATH="${HOME}/.git-aliases:${PATH}"

if [ -f ~/.bash_ros_aliases ]; then
  . ~/.bash_ros_aliases
fi

if [ -f ~/roscorizing/scripts/roscorizing.sh ]; then
  . ~/roscorizing/scripts/roscorizing.sh
fi
